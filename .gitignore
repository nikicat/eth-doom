# third-party reference clones (id Software Wolf3D source is non-free — do NOT
# redistribute; re-create with the clone commands in reference/README.md)
/reference/

# build artifacts
/contracts/out/
/contracts/cache/
/contracts/broadcast/
target/
node_modules/
/web/dist/
/oracle/build/
/renderer/build/
/audio/build/
*.wasm
/web/public/wolfrender.mjs

# third-party OPL2 emulator source (Nuked-OPL3, LGPL-2.1) fetched by audio/build_opl.sh —
# NOT committed, like reference/ (re-fetch with the build script). The decoded audio
# itself (digi WAV / AdLib / IMF) lands under /web/public/wolf/, already ignored above.
/audio/vendor/

# id Software shareware assets — DO NOT COMMIT (read from a user-provided dir)
/assets/
*.WL1
*.wl1
GAMEMAPS*
MAPHEAD*
VSWAP*
VGAGRAPH*
VGADICT*
VGAHEAD*
# textures/sprites decoded from a user-provided VSWAP by `wl-extract` (id art)
/web/public/wolf/
/web/public/level.json

# verification artifacts
/.playwright-mcp/
/ethdoom-*.png

# editor / os
.DS_Store
*.swp
